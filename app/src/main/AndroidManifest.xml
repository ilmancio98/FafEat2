<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fafeat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FafEat">
        <activity android:name=".LoginSignup.ForgetPasswordSuccessMassage"></activity>
        <activity android:name=".LoginSignup.SetNewPassword" />
        <activity android:name=".LoginSignup.Make_selection" />
        <activity android:name=".LoginSignup.ForgetPassword" />
        <activity android:name=".LoginSignup.Signupcust2" />
        <activity android:name=".LoginSignup.SignupCust" />
        <activity android:name=".LoginSignup.Chooseut" />
        <activity android:name=".LoginSignup.Login" />
        <activity
            android:name=".LoginSignup.FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.FafEat.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>